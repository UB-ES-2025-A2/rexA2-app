name: Build and deploy backend to Azure Web App - rex

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      # 1️⃣ Descargar código
      - name: Checkout
        uses: actions/checkout@v4

      # 2️⃣ Preparar Python para validar dependencias del backend
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install backend dependencies (sanity check)
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 3️⃣ Desplegar TODO el repo (incluyendo la carpeta backend)
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'rex'
          publish-profile: ${{ secrets.AZUREAPPSERVICEPUBLISHPROFILE_REX }}
          package: .



